{# templates/emails/reset_password_admin.html.twig #}
<p>Bonjour {{ user.firstName ?? user.email }},</p>

<p>Un administrateur a demandé la réinitialisation de votre mot de passe.</p>

<p>
  <a href="{{ url('app_reset_password', { token: resetToken.token }) }}">
    Cliquez ici pour définir un nouveau mot de passe
  </a>
</p>

<p>
  Ce lien expire le {{ resetToken.expiresAt|date('d/m/Y H:i') }}
  ({{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}).
</p>

<p>Si vous n’êtes pas à l’origine de cette demande, ignorez cet e-mail.</p>

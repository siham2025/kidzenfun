{% extends 'base.html.twig' %}

{% block title %}E-mail envoyé{% endblock %}

{% block body %}
  <div class="container" style="max-width:720px;margin:32px auto;">
    <h1>E-mail de réinitialisation envoyé</h1>

    <p>
      S’il existe un compte pour cette adresse, nous venons d’envoyer un e-mail contenant un lien
      pour réinitialiser votre mot de passe.
      Ce lien expirera
      {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>

    <p>
      Si vous ne recevez pas l’e-mail, vérifiez vos spams ou
      <a href="{{ path('app_forgot_password_request') }}">demandez un nouveau lien</a>.
    </p>

    <div class="buttons" style="margin-top:12px;">
      <a href="{{ path('app_login') }}" class="btn purple">Aller à la connexion</a>
    </div>
  </div>
{% endblock %}
{% extends 'base.html.twig' %}
{% block title %}FAQ – Kidzen&Fun{% endblock %}

{% block body %}
<section class="faq">
  <div class="faq-container">
    <h1 class="faq-title">Foire aux questions</h1>
    <p class="faq-intro">
      Tu trouveras ici les réponses aux questions les plus fréquentes sur Kidzen&Fun.
      Si quelque chose manque, <a href="{{ path('app_contact') }}">contactez-nous</a> 🤗
    </p>

    <div class="faq-list">
      {# On boucle sur les FAQ envoyées par le contrôleur #}
      {% for item in faqs %}
        <details class="faq-item" {{ loop.first ? 'open' }}>
          <summary class="faq-question">{{ item.question }}</summary>
          <div class="faq-answer">
            {{ item.answer|raw }} {# |raw pour autoriser un peu de HTML dans la réponse #}
          </div>
        </details>
      {% else %}
        <p>Aucune question pour le moment.</p>
      {% endfor %}
    </div>
  </div>
</section>
{% endblock %}
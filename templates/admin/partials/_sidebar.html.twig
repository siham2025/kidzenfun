
<aside class="admin-sidebar">
  <div class="sidebar-header">
    <a href="{{ path('home') }}" class="logo">
      <i class="fas fa-sun"></i> Admin
    </a>
  </div>

  <nav class="sidebar-nav">
    <ul>
      <li class="{{ app.request.attributes.get('_route') starts with 'admin_dashboard' ? 'active' : '' }}">
        <a href="{{ path('admin_dashboard') }}"><i class="fas fa-home"></i> Tableau de bord</a>
      </li>

      <li class="{{ app.request.attributes.get('_route') starts with 'admin_users' ? 'active' : '' }}">
        <a href="{{ path('admin_users') }}"><i class="fas fa-users"></i> Gestion des utilisateurs</a>
      </li>

      <li class="{{ app.request.attributes.get('_route') starts with 'admin_activities' ? 'active' : '' }}">
        <a href="{{ path('admin_activities') }}"><i class="fas fa-palette"></i> Gestion des activités</a>
      </li>

      <li class="{{ app.request.attributes.get('_route') starts with 'admin_faq' ? 'active' : '' }}">
        <a href="{{ path('admin_faq_index') }}"><i class="fas fa-question-circle"></i> Gestion de la FAQ</a>
      </li>
    </ul>
  </nav>

  <div class="sidebar-footer">
{# firewall est configuré pour le GET /logout #}
    <a href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
   
    
  </div>
</aside>

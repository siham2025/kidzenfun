{{ form_start(form) }}
  <div class="form-grid">
    {{ form_row(form.question) }}
    {{ form_row(form.answer) }}
    {{ form_row(form.category) }}
    {{ form_row(form.position) }}
    {{ form_row(form.isPublished) }}
    {{ form_row(form.locale) }}
  </div>

  <button class="btn purple mt-3">Enregistrer</button>
{{ form_end(form) }}
